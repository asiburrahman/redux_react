import React from 'react';

const SingleTodo = ({todo}) => {
          console.log(todo);
    return (
  
        
        <div>
            {todo}
        </div>
    );
};

export default SingleTodo;